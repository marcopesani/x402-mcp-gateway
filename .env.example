# =============================================================================
# PayMCP Environment Variables
# Copy this file to .env.local and fill in the values.
# =============================================================================

# ---------------------------------------------------------------------------
# Chain / Network
# ---------------------------------------------------------------------------
# Chain ID for the target network. Defaults to 8453 (Base Mainnet).
# Set to 84532 for Base Sepolia testnet.
NEXT_PUBLIC_CHAIN_ID=8453

# ---------------------------------------------------------------------------
# WalletConnect / Reown AppKit
# ---------------------------------------------------------------------------
# Project ID for WalletConnect integration (required for wallet connection).
# Get yours at: https://dashboard.reown.com
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# ---------------------------------------------------------------------------
# Alchemy (optional)
# ---------------------------------------------------------------------------
# Alchemy API key for enhanced RPC access. Optional — the app works without it.
# Sign up at: https://dashboard.alchemy.com
NEXT_PUBLIC_ALCHEMY_ID=

# ---------------------------------------------------------------------------
# Base Network RPC
# ---------------------------------------------------------------------------
# Custom RPC URL for the Base network. If not set, defaults to the public Base RPC.
# The public RPC is strictly rate-limited (429); set a dedicated URL to avoid balance/withdraw failures.
# Recommended: Alchemy, Infura, QuickNode, or https://www.base.org/developers (free tier).
# Example: https://base-mainnet.g.alchemy.com/v2/YOUR_KEY
RPC_URL=

# ---------------------------------------------------------------------------
# Hot Wallet Encryption
# ---------------------------------------------------------------------------
# 256-bit encryption key for hot wallet private key storage (required).
# Must be a 64-character hex string (32 bytes).
# Generate one with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
HOT_WALLET_ENCRYPTION_KEY=

# ---------------------------------------------------------------------------
# JWT Session
# ---------------------------------------------------------------------------
# Secret key for signing JWT session tokens (required).
# Use a strong random string (at least 32 characters).
# Generate one with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=

# ---------------------------------------------------------------------------
# Database
# ---------------------------------------------------------------------------
# Postgres connection string.
# Example: postgresql://user:password@host:5432/dbname
DATABASE_URL=postgresql://user:password@host:5432/dbname
